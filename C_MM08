#include<stdio.h>

int main(int argc, char *argv[]) {
    long long int up, down;

    // 使用 %f 與變數型別對應
    while (scanf("%lld%lld", &up,&down) != EOF) {
        printf("%lld\n",(up+down)*(up+down));
    }

    return 0;
}
