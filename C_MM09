#include <stdio.h>

// 計算 2 的 i 次方值
int main() {
    int i;
    scanf("%d",&i);
    if (i >= 31) {
        printf("Value of more than 31\n");
    } else {
        int result = 1 << i;
        printf("%d\n", result);
    }

    return 0;
}
