#include<stdio.h>
int main(){
    int n=0,sum=0;
    scanf("%d",&n);
    for(int i=1;i<=n;i++){
        sum+=i;

        if(i==n){
            printf("%d = %d\n",i,sum);
        }
        else if(i==1) printf("%d + ",i);
        else{
            printf("%d + ",i);
        }
    }
}
