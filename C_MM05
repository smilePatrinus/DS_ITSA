#include<stdio.h>
#include<math.h>
int main(int argc, char *argv[]) {
    double up, down, tall;

    // 使用 %f 與變數型別對應
    while (scanf("%lf", &up) != EOF) {
        up*=up;
        printf("%.1f\n", round(up * 10) / 10);
    }

    return 0;
}
