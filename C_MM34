#include <stdio.h>
#include <stdlib.h>

int main() {
    int n=0;
    scanf("%d",&n);
    for(int i=1;i<=n;i++){
        if(i==1) printf("%d",i);
        else if(i==n) printf(" %d\n",i);
        else if(n%i==0){
            printf(" %d",i);
        }
    }




	return 0;
}
