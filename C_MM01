#include<stdio.h>

int main(int argc, char *argv[]) {
    float up, down, tall;

    // 使用 %f 與變數型別對應
    while (scanf("%f %f %f", &up, &down, &tall) != EOF) {
        printf("Trapezoid area:%.1f\n", (up + down) * tall / 2);
    }

    return 0;
}
